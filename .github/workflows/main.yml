name: "online-chess workflow"
on: 
  push:
    branches: ["main"]
jobs:
  compose:
    runs-on: [holland, linux, ARM64]
    steps:
      - run: git pull
      - run: docker-compose down
      - run: docker-compose up
